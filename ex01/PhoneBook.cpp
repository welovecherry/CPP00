#include "PhoneBook.hpp"
#include <iostream>

/*
기본 생성자 선언은 `.hpp` 파일에 위치하고, 그 구현(정의)은 `.cpp` 파일에 위치하는 것이 일반적인 관례야. `.hpp` 파일에는 클래스의 인터페이스(멤버 함수와 변수의 선언)가 포함되고, `.cpp` 파일에는 이러한 멤버 함수들의 실제 동작을 정의하는 코드가 들어가. 이렇게 분리함으로써 코드의 가독성과 관리가 용이해져.
*/

